namespace DropWord.TgBot.Di.Manager;

public class ManagerBuild
{
    public static void BuildService(IServiceCollection services)
    {
        
        // services.AddTransient<DropWordContext>();
        // services.AddTransient<IStateTreeManager, StateTreeManager>();
        // services.AddTransient<IStateTreeRepository, StateTreeRepository>();
    }
}